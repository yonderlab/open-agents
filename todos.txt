- [x] add reason to tool approval response
- [x] move to use context (remove prop drilling)
 - [x] can store model id in context and then update with hooks?
- [x] update common types file to also have agent definition
- [x] show stream of subagent
- [x] use prune messages!
- [x] move subagent definition into it's own file
- [x] use toModelOutput to return just the last message / text part from the task tool. (update the system prompt to say summarize your work.)
- [x] update folders to be (fs, planning, memory, task)
- [x] cancel requests with esc
- [ ] pull in and cache context window size with models.dev
- [ ] implement checkpointing for auto compaction (docs/auto-compaction-checkpoint-idea.md)
- [x] implement reasoning support (ui and provider options)
- [ ] add approved commands (auto approve for similar commands)
- [ ] update task branch when git checkout runs
- [x] audit the input implementation (suggestions not working and missing chars)
- [x] support for pasting images
- [ ] update system prompt to lean heavily on subagents
- [ ] review (and probably remove) all old deep agent tools / instructions (scratchpad)
    - [ ] explore implementation that doesn't require sandbox? virtual fs
- [x] review todos/scratchpad in system prompt
    - if used, we should use prepareStep to build system prompt again (with this context)
- [x] add agents.md support
- [x] when pasting more than 3 lines in, we should collapse like other agents
- [x] when going through suggestions, you should be able to use ctrl+n to go through to hidden files
- [ ] command+enter should only delete until the end of the line
- [x] sandbox adapter (sandbox.readFile) -> could be mapped to native fs or sandbox command (can live on the experimental_context too)
- [ ] create action bullet has no space... 
- [ ] add some kind of check for editing tools blocking if it hasn't been read first!  (cc claude code)
- [x] update action / command ui to be white when pending (not orange)
- [ ] explore a tool registry that uses provider defined tools when available...
- [ ] explore tool registry (server-side) that switches to provider-defined tools when available
- [ ] add slash commands
 -  I want to add support for slash commands in the CLI, or the TUI. I have attached two images that show exactly how they should work, but when you hit slash, it should show the
  similar suggestions UI for autocompleting and navigating through slash commands, and then when you click one, it should open a settings panel to set that specific setting.
- [ ] add ability to alt arrow between input box
- [ ] interrupt conversation (stop) if no reason is passed to approval request 
- [x] diff viewer shouldn't show diff if creating file! 
- [ ] add askUserQuestion tool!

- [ ] audit call options
- [ ] add ability to save settings per project / or per user (~/.settings)

- [ ] introduce concept of user approval and machine approval
  - there are things that should have an auto approve / reject
  - e.g. subagent running a command it shouldnt, should just reject the tool call (add tool result)

- [ ] change button to be create repo if no repo exists
  - also need to add support for starting up sandbox with no repo / branch


---
sandbox related:
- [ ] /slash command for selecting sandbox
- [ ] add lifecycle hooks for sandbox interface (after setup / before stop)
- [ ] add timeout related info to interface
- [ ] add command to ssh into sandbox!
- [ ] update suggestions (tui) to show files in sandbox
- [ ] always start agent in auto accept mode if using sandbox
- [ ] if we expose the timeout, could explore forcing a tool call at a certain point that is like, extend sandbox or push changes
- [ ] add base url so that we can see dev server

- have tabs for either starting from an existing repo or fresh (empty sandbox)
- have concept of workspaces which are sandboxes that start (and tabs in the sidebar) - e.g. the equivalent of chats
- like conductor, each workspace could have multiple chats? so we could referense one, that would be cool

---
Web

- [ ] readFile tool should be red if a file hasn't been found!

---


---

Turn this into full OSS end-to-end CC
- [ ] cli coding agent
- [ ] web ui (background agents with sandbox) - https://github.com/vercel-labs/coding-agent-template
- [ ] slack bot
- [ ] durable agent support (create my own wrapper which is const agent = durable(agent); agent.stream())
